---
layout:  base
title: Blog
---

<style>
  .image-wrapper img {
    display: block;
    width: 100%;
    max-width: 100%;
    height: 220px;
    border-radius: 0.75rem;
    box-shadow: 0 0.25rem 0.375rem -0.0625rem rgb(20 20 20 / 12%), 0 0.125rem 0.25rem -0.0625rem rgb(20 20 20 / 7%);
  }

  li,
  ul {
    list-style: none;
    padding: 0;
  }

  a:hover h5 {
    color: var(--bs-primary);
    transition: color 0.25s ease-out;
  }
</style>

{% include "navbar-light.njk" %}

<header class="my-6">
  <div class="page-header">
    <div class="container">
      <div class="col-lg-8 text-center mx-auto my-auto">
        <h1>Blog</h1>
        <p class="lead">We’re constantly trying to express ourselves and actualize our dreams. If you have the opportunity to play this game</p>
      </div>
    </div>
  </div>
</header>

<div class="container mb-8">
  <ul class="row g-4">
    {% for post in prismicData.blogposts %}
      <li class="col-lg-4 d-flex align-items-stretch">
        <div class="card shadow-lg">
          <div class="card-header p-0 mx-3 mt-3">
            <div class="d-block blur-shadow-image shadow">
              <a href="/blog/{{ post.uid | slug }}">
                {% Image post.data.article_image.url,
                "image" %}
              </a>
            </div>
          </div>

          <div class="card-body d-flex flex-column">
            <a href="/blog/{{ post.uid | slug }}">
              <h5 class="mt-3">{{post.data.title | prismicText}}</h5>
            </a>

            <p class="mt-3">
              {{ post.data.body | prismicHtml | excerpt }}
            </p>

            <div class="mt-auto">
              <small>{{ post.data.date | prismicDate | postDate }}</small>
            </div>
          </div>
        </div>
      </li>
    {% endfor %}
  </ul>
</div>

{% include "footer-dark.njk" %}

<script type="module" async>
  import Lazyload from "https://cdn.skypack.dev/vanilla-lazyload";
  const lazyload = new Lazyload();
</script>